var fromPlaces = ["Chicago", "New York", "Shanghai", "Hangzhou"];

function chooseFrom() {
	mountChoosePane("from");
}

function chooseTo() {
	mountChoosePane("to");
}

function chooseDate() {
	mountChoosePane("date");
}

function mountChoosePane(type) {
	if(type == "from") {
		;
	} else if (type == "to") {
		;
	} else if (type == "date") {
		;
	}
}
